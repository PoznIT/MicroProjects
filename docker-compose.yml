services:
  auth:
    build: ./auth
    restart: unless-stopped
    env_file: .env

  proxy:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - .:/var/www:ro
    depends_on:
      - auth
    restart: unless-stopped
